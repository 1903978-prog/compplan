node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.replit
.local/
.agents/
.config/
attached_assets/
.claude/

# Local env files — never commit real secrets
.env
.env.local
.env.*.local

# Transient debug / scratch files that have slipped into commits
ppt-generation-brief.json
*.tmp.json

# Backup secrets and dumps
.env.backup
*.dump
*.dump.gz
*.bundle
*.bundle.gz
*.sha256
*.pem
*.p12
credentials.json

# Local dev runtime artifacts (server logs, install logs, throwaway scripts)
server_err.txt
server_out.txt
server_err.log
server_out.log
.npm-install.log
dev.cmd
kill-locks.cjs
