services:
  - type: web
    name: compplan
    runtime: node
    buildCommand: npm run build
    startCommand: npm run db:push && npm start
    envVars:
      - key: NODE_ENV
        value: production
