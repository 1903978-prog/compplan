services:
  - type: web
    name: compplan
    runtime: node
    buildCommand: npm run build && npm run db:push
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
